apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: authentication-ingress
  namespace: mosip-ida
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/cors-allow-origin: "*"
    nginx.ingress.kubernetes.io/cors-allow-methods: "GET, POST, PUT, DELETE, OPTIONS"
    nginx.ingress.kubernetes.io/cors-allow-headers: "DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization"
    nginx.ingress.kubernetes.io/cors-allow-credentials: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
spec:
  rules:
    - host: authentication.mosip.local
      http:
        paths:
          # Authentication Service
          - path: /v1/auth
            pathType: Prefix
            backend:
              service:
                name: authentication-service
                port:
                  number: 8090

          # OTP Service
          - path: /v1/otp
            pathType: Prefix
            backend:
              service:
                name: authentication-otp-service
                port:
                  number: 8090

          # Internal Service
          - path: /v1/internal
            pathType: Prefix
            backend:
              service:
                name: authentication-internal-service
                port:
                  number: 8090

          # Health Check endpoints
          - path: /actuator/health
            pathType: Prefix
            backend:
              service:
                name: authentication-service
                port:
                  number: 8090

          # Management endpoints
          - path: /actuator
            pathType: Prefix
            backend:
              service:
                name: authentication-service
                port:
                  number: 8090

          # Default route to main authentication service
          - path: /
            pathType: Prefix
            backend:
              service:
                name: authentication-service
                port:
                  number: 8090

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: authentication-ingress-tls
  namespace: mosip-ida
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/cors-allow-origin: "*"
    nginx.ingress.kubernetes.io/cors-allow-methods: "GET, POST, PUT, DELETE, OPTIONS"
    nginx.ingress.kubernetes.io/cors-allow-headers: "DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization"
    nginx.ingress.kubernetes.io/cors-allow-credentials: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
    - hosts:
        - authentication.mosip.example.com
      secretName: authentication-tls
  rules:
    - host: authentication.mosip.example.com
      http:
        paths:
          # Authentication Service
          - path: /v1/auth
            pathType: Prefix
            backend:
              service:
                name: authentication-service
                port:
                  number: 8090

          # OTP Service
          - path: /v1/otp
            pathType: Prefix
            backend:
              service:
                name: authentication-otp-service
                port:
                  number: 8090

          # Internal Service
          - path: /v1/internal
            pathType: Prefix
            backend:
              service:
                name: authentication-internal-service
                port:
                  number: 8090

          # Health Check endpoints
          - path: /actuator/health
            pathType: Prefix
            backend:
              service:
                name: authentication-service
                port:
                  number: 8090

          # Management endpoints
          - path: /actuator
            pathType: Prefix
            backend:
              service:
                name: authentication-service
                port:
                  number: 8090

          # Default route to main authentication service
          - path: /
            pathType: Prefix
            backend:
              service:
                name: authentication-service
                port:
                  number: 8090
